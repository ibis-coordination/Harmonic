<%# Settings sidebar variant - minimal sidebar for settings pages %>
<%# Shows only collective info and back link %>
<aside class="pulse-sidebar" data-mode="settings">
  <%= render 'pulse/sidebar_collective_info' %>

  <nav class="pulse-nav">
    <a href="<%= @current_collective.path %>" class="pulse-nav-item">
      <%= octicon 'arrow-left', height: 16 %>
      <span>Back to <%= @current_collective.private_workspace? ? "Workspace" : "Collective" %></span>
    </a>
  </nav>
</aside>
