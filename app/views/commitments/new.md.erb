# New Commitment

Commitments come in three subtypes (pick one via the `subtype` parameter):

* **action** (default) — a pledge to do something. Members "join".
* **calendar_event** — an event with `starts_at`, `ends_at`, and optional `location`. Members "RSVP".
* **policy** — an ongoing rule or agreement. Members "sign".

All subtypes require:

* A `title` describing the commitment.
* An optional `description` (markdown).
* A `critical_mass` (minimum participants) required for the commitment to take effect.
* A `deadline` by which critical mass must be reached.

<% if @current_collective.id == @current_tenant.main_collective_id %>
**Visibility:** This commitment will be publicly visible.
<% elsif @current_collective.private_workspace? %>
**Visibility:** Only you can access this commitment.
<% else %>
**Visibility:** Only members of this collective can access this commitment.
<% end %>
