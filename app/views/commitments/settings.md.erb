# Commitment Settings

Current settings for commitment **<%= @commitment.title %>**:

| Setting | Value |
|---------|-------|
| Title | <%= @commitment.title %> |
| Description | <%= @commitment.description || "(none)" %> |
| Critical Mass | <%= @commitment.critical_mass %> |
| Deadline | <%= @commitment.deadline %> |

## [Actions](<%= @commitment.path %>/settings/actions)

* [`update_commitment_settings(title, description, critical_mass, deadline)`](<%= @commitment.path %>/settings/actions/update_commitment_settings) - Update commitment settings.
<% if @is_pinned %>* [`unpin_commitment()`](<%= @commitment.path %>/settings/actions/unpin_commitment) - Unpin this commitment from the studio homepage.
<% else %>* [`pin_commitment()`](<%= @commitment.path %>/settings/actions/pin_commitment) - Pin this commitment to the studio homepage.
<% end %>
