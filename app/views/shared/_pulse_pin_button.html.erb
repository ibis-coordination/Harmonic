<%# Wrapper partial - delegates to PinButtonComponent. %>
<%# Existing callers can continue using: render "shared/pulse_pin_button", resource: @note, is_pinned: @is_pinned %>
<%# New code should use: render PinButtonComponent.new(resource: @note, is_pinned: @is_pinned) %>
<%= render PinButtonComponent.new(
  resource: resource,
  is_pinned: is_pinned,
) %>
