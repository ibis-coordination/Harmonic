<%# Wrapper partial - delegates to ResourceLinkComponent. %>
<%# Existing callers can continue using: render "shared/pulse_resource_link", resource: @note %>
<%# New code should use: render ResourceLinkComponent.new(resource: @note) %>
<%= render ResourceLinkComponent.new(
  resource: resource,
) %>
