<%# Wrapper partial - delegates to BreadcrumbComponent. %>
<%# Existing callers can continue using: render "shared/pulse_breadcrumb", items: [...] %>
<%# New code should use: render BreadcrumbComponent.new(items: [...]) %>
<%= render BreadcrumbComponent.new(
  items: items,
) %>
