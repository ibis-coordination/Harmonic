<%# Wrapper partial - delegates to TooltipComponent. %>
<%# Existing callers can continue using: render("shared/tooltip") { "content" } %>
<%# New code should use: render(TooltipComponent.new) { "content" } %>
<%= render TooltipComponent.new do %>
  <%= yield %>
<% end %>
