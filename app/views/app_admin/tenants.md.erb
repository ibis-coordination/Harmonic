# All Subdomains

[Back to App Admin](/app-admin)

<%= @tenants.count %> subdomains total

| Name | Subdomain | Status |
|------|-----------|--------|
<% @tenants.each do |tenant| -%>
| [<%= tenant.name %>](/app-admin/subdomains/<%= tenant.subdomain %>) | `<%= tenant.subdomain %>` | <%= tenant.subdomain == ENV['PRIMARY_SUBDOMAIN'] ? 'primary' : '' %> |
<% end -%>

## Actions

* [Create New Subdomain](/app-admin/subdomains/new)
