<%
base_path = "/u/#{@target_user.handle}/settings/trustee-grants"
-%>
# Create New Trustee Grant

Grant another user authority to act on your behalf. They will need to accept the trustee grant before they can use it.

## Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| trustee_user_id | string | yes | The ID of the user to grant authority to |
| permissions | array | yes | Array of action names to grant |
| collective_scope_mode | string | no | Collective scope: "all" (default), "include", or "exclude" |
| collective_ids | array | no | Array of collective IDs for include/exclude modes |
| expires_at | datetime | no | When the trustee grant expires |

## Grantable Actions

<% @grantable_actions.each do |action_name| -%>
<% action_def = ActionsHelper::ACTION_DEFINITIONS[action_name] -%>
* `<%= action_name %>` - <%= action_def&.dig(:description) || action_name.humanize %>
<% end -%>

## Available Users

<% if @available_users.any? -%>
| ID | Name | Handle | Type |
|----|------|--------|------|
<% @available_users.each do |user| -%>
| `<%= user.id %>` | <%= user.display_name || user.name %> | <%= user.handle %> | <%= user.user_type %> |
<% end -%>
<% else -%>
*No users available for trustee grants.*
<% end -%>

## Collective Scope Modes

| Mode | Description |
|------|-------------|
| `all` | All collectives where you are a member (default) |
| `include` | Only the specified collectives |
| `exclude` | All collectives except the specified ones |

## Available Collectives

<% if @available_collectives.any? -%>
| ID | Name |
|----|------|
<% @available_collectives.each do |collective| -%>
| `<%= collective.id %>` | <%= collective.name %> |
<% end -%>
<% else -%>
*No collectives available.*
<% end -%>

## Navigation

* [All Trustee Grants](<%= base_path %>)
<% if @target_user == @current_user -%>
* [Settings](/settings)
<% else -%>
* [<%= @target_user.handle %>'s Profile](/u/<%= @target_user.handle %>)
<% end -%>
