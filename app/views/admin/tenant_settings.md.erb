# Tenant Settings

[Back to Admin Dashboard](/admin)

Current settings for **<%= @current_tenant.name %>**:

| Setting | Value |
|---------|-------|
| Name | <%= @current_tenant.name %> |
| Timezone | <%= @current_tenant.timezone %> |
| Require Login | <%= @current_tenant.require_login? ? 'Yes' : 'No' %> |
| Allow File Uploads | <%= @current_tenant.allow_file_uploads? ? 'Yes' : 'No' %> |
| API Enabled | <%= @current_tenant.api_enabled? ? 'Yes' : 'No' %> |

## [Actions](/admin/settings/actions)

<% if can_perform_admin_actions? %>
* [`update_tenant_settings(name, timezone, require_login, allow_file_uploads, api_enabled)`](/admin/settings/actions/update_tenant_settings) - Update tenant settings
<% else %>
* No actions available (read-only access)
<% end %>
