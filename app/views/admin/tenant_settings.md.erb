# Tenant Settings

[Back to Admin Dashboard](/admin)

Current settings for **<%= @current_tenant.name %>**:

| Setting | Value |
|---------|-------|
| Name | <%= @current_tenant.name %> |
| Timezone | <%= @current_tenant.timezone %> |
| Require Login | <%= @current_tenant.require_login? ? 'Yes' : 'No' %> |
<% available_flags = FeatureFlagService.all_flags.select { |f| FeatureFlagService.app_enabled?(f) } %>
<% if available_flags.any? %>

## Features Enabled

Features must be enabled at the tenant level for studios to use them.

| Feature | Enabled |
|---------|---------|
<% available_flags.each do |flag_name| %>
<% metadata = FeatureFlagService.flag_metadata(flag_name) %>
<% tenant_enabled_locally = @current_tenant.feature_flag_enabled_locally?(flag_name) %>
| <%= metadata['name'] %> | <%= tenant_enabled_locally ? 'Yes' : 'No' %> |
<% end %>
<% end %>

