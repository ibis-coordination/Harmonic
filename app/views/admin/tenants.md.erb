# All Tenants

[Back to Admin Dashboard](/admin)

| Subdomain | Name |
|-----------|------|
<% @tenants.each do |tenant| -%>
| [`<%= tenant.subdomain %>`](/admin/tenants/<%= tenant.subdomain %>) | <%= tenant.name %> |
<% end -%>

