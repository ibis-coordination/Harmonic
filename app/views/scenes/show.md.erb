# Scene: <%= @current_superagent.name %>

<%= @current_superagent.description %>

## Recent Notes

<% if @notes.length > 0 %>
<% @notes.each do |note| %>
* [<%= note.title.present? ? note.title : "Note #{note.truncated_id}" %>](<%= note.path %>)
  * Created: <%= time_ago_in_words(note.created_at) %> ago
<% end %>
<% else %>
No notes yet.
<% end %>

