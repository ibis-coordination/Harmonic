<div class="pulse-auth-page">
  <div class="pulse-auth-logo">
    <i class="tri-logo-icon icon-lg"></i>
  </div>

  <h1 class="pulse-auth-title">Reset password</h1>

  <p class="pulse-auth-subtitle">
    Enter your email address and we'll send you instructions to reset your password.
  </p>

  <%= form_with url: password_resets_path, method: :post, data: { turbo: false }, class: "pulse-auth-form" do |form| %>
    <%= render "shared/bot_protection_fields" %>
    <%= form.email_field :email, placeholder: 'Email', required: true, class: 'pulse-form-input' %>
    <%= render "shared/turnstile_widget" %>
    <div class="pulse-form-actions">
      <%= submit_tag "Send reset instructions", class: "pulse-action-btn", style: "width:100%;" %>
    </div>
    <div class="pulse-auth-footer">
      <%= link_to 'Back to login', '/login', class: 'pulse-auth-link' %>
    </div>
  <% end %>
</div>
