<h4 style="display:inline-block;margin-top:0;">
  <%= link_to @current_studio.name, @current_studio.path || '/' %>
  / <i class="note-icon"></i>
  Note <code><%= @note.truncated_id %></code>
  / Edit
</h4>
<%= form_with do |form| %>
  <div>
    <%= form.text_area :text, placeholder: 'Note', style: 'height:300px;', value: @note.text %>
  </div>
  <hr style="height:1px;" />
  <p>
    Update events will appear in note history.
  </p>
  <%= form.submit 'Save note' %>
<% end %>
