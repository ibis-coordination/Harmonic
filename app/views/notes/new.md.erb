# New Note

Notes require a title, text content (markdown), and a deadline. The deadline determines the relevance duration of the note.

## Actions

* `create_note(title="...", text="...", deadline="YYYY-MM-DD")` to create a new note.
