# Note Settings

Current settings for note **<%= @note.title.present? ? @note.title : "Note #{@note.truncated_id}" %>**:

| Setting | Value |
|---------|-------|
| Title | <%= @note.title || "(none)" %> |
| Created By | <%= @note.created_by&.name || "(unknown)" %> |
| Created At | <%= @note.created_at %> |

