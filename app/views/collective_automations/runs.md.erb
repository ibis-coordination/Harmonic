# Run History: <%= @automation_rule.name %>

<% if @runs.empty? -%>
*No runs recorded yet.*

Runs will appear here once this automation is triggered.
<% else -%>
| Time | Status | Trigger | Source IP | Details |
|------|--------|---------|-----------|---------|
<% @runs.each do |run| -%>
| <%= time_ago_in_words(run.created_at) %> ago | <%= run.status %> | <%= run.trigger_source %> | <%= run.trigger_data&.dig("source_ip") || "-" %> | <% if run.error_message.present? %><%= truncate(run.error_message, length: 30) %><% else %>-<% end %> |
<% end -%>
<% end -%>

## Navigation

* [Back to Automation](<%= automation_path(@automation_rule) %>)
* [Back to Automations](<%= automations_index_path %>)
