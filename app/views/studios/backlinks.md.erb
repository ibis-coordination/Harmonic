# Backlinks

<% if @backlinks.empty? %>No backlinks yet.<% else %>
|          |          | backlinks |
| -------- | -------- | --------- |
<% @backlinks.each.with_index do |bl, i| %><% resource, count = bl %>| <%= i + 1 %> | [<%= resource.title %>](<%= resource.path %>) | <%= count %> |
<% end %>
<% end %>