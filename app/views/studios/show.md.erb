# Studio: <%= @current_studio.name %>

## Explore

* [Cycles](<%= @current_studio.path %>/cycles)
* [Backlinks](<%= @current_studio.path %>/backlinks)

## Pinned

<% if @pinned_items.length > 0 %>
<% @pinned_items.each do |item| %>
* <%= item[:item].class.to_s %>: [<%= item[:item].title %>](<%= item[:item].path %>) pinned <%= time_ago_in_words(item[:pinned_at]) %> ago
<% end %>
<% else %>
No pinned items yet.
<% end %>

## Team

<% if @current_studio.users.length > 0 %>
<% @current_studio.users.each do |user| %>
* [<%= user.display_name %>](<%= user.path %>)
<% end %>
<% else %>
No team members yet.
<% end %>

## Actions

* [New Note](<%= @current_studio.path %>/note)
* [New Decision](<%= @current_studio.path %>/decide)
* [New Commitment](<%= @current_studio.path %>/commit)
