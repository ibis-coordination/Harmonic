# Studio: <%= @current_superagent.name %>

<% if !@current_superagent.is_main_superagent? && @current_user && !@current_heartbeat -%>
## Heartbeat Required

You must send a heartbeat to access this studio for the current cycle.

## [Actions](<%= @current_superagent.path %>/actions)

* [`send_heartbeat()`](<%= @current_superagent.path %>/actions/send_heartbeat) - Send a heartbeat to confirm your presence in the studio
<% else -%>
## Explore

* [Cycles](<%= @current_superagent.path %>/cycles)
* [Backlinks](<%= @current_superagent.path %>/backlinks)

## Pinned

<% if @pinned_items.length > 0 -%>
<% @pinned_items.each do |item| -%>
* <%= item[:item].class.to_s %>: [<%= raw item[:item].title %>](<%= item[:item].path %>) pinned <%= time_ago_in_words(item[:pinned_at]) %> ago
<% end -%>
<% else -%>
No pinned items yet.
<% end -%>

## Team

<% if @current_superagent.users.length > 0 -%>
<% @current_superagent.users.each do |user| -%>
* <%= user_link_md(user) %>
<% end -%>
<% else -%>
No team members yet.
<% end -%>

## [Actions](<%= @current_superagent.path %>/actions)

* [New Note](<%= @current_superagent.path %>/note)
* [New Decision](<%= @current_superagent.path %>/decide)
* [New Commitment](<%= @current_superagent.path %>/commit)
<% end -%>
