<%= render 'shared/pulse_breadcrumb', items: [
  ['Home', '/'],
  'Collectives'
] %>

<article class="pulse-resource-detail">
  <header class="pulse-resource-header">
    <div class="pulse-resource-header-top">
      <span class="pulse-resource-type-label">
        <%= octicon 'heart', height: 14, class: 'pulse-resource-icon' %>
        Collectives
      </span>
    </div>
    <h1 class="pulse-resource-title">Your Collectives</h1>
  </header>

  <div class="pulse-resource-body">
    <% if @current_tenant.feature_enabled?("stripe_billing") %>
      <% if @my_collectives&.any? %>
        <section class="pulse-settings-section">
          <h2 class="pulse-section-title">Created by you</h2>
          <ul class="pulse-home-list" style="list-style-type: none;">
            <% @my_collectives.each do |collective| %>
              <%= render partial: 'collectives/collective_list_item', locals: { collective: collective } %>
            <% end %>
          </ul>
        </section>
      <% end %>

      <% if @member_collectives&.any? %>
        <section class="pulse-settings-section">
          <h2 class="pulse-section-title">Member of</h2>
          <ul class="pulse-home-list" style="list-style-type: none;">
            <% @member_collectives.each do |collective| %>
              <%= render partial: 'collectives/collective_list_item', locals: { collective: collective } %>
            <% end %>
          </ul>
        </section>
      <% end %>

      <% if !@my_collectives&.any? && !@member_collectives&.any? && @current_user %>
        <p class="pulse-empty-content"><em>You are not a member of any collectives yet.</em></p>
      <% end %>
    <% else %>
      <% if @collectives.any? %>
        <ul class="pulse-home-list" style="list-style-type: none;">
          <% @collectives.each do |collective| %>
            <%= render partial: 'collectives/collective_list_item', locals: { collective: collective } %>
          <% end %>
        </ul>
      <% elsif @current_user %>
        <p class="pulse-empty-content"><em>You are not a member of any collectives yet.</em></p>
      <% else %>
        <p class="pulse-empty-content">
          <a href="/login" class="pulse-link">Log in</a> to see your collectives.
        </p>
      <% end %>
    <% end %>

    <% if @current_user %>
      <div class="pulse-form-actions" style="margin-top: 1.5rem;">
        <a href="/collectives/new" class="pulse-action-btn">
          <%= octicon 'plus', height: 14 %>
          Create New Collective
        </a>
      </div>
    <% end %>
  </div>
</article>
