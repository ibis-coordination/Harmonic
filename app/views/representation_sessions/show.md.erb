# Representation Session `<%= @representation_session.truncated_id %>`

<% unless @representation_session.ended_at -%>
**This session is currently active.**
<% end -%>

| | |
|---|---|
| Representative | [<%= raw @representation_session.representative_user.display_name %>](<%= @representation_session.representative_user.path %>) |
| Started | <%= time_ago_in_words(@representation_session.began_at) %> ago |
<% if @representation_session.ended_at -%>
| Ended | <%= time_ago_in_words(@representation_session.ended_at) %> ago |
<% duration = (@representation_session.elapsed_time / 60).round -%>
| Duration | <%= duration %> minute<%= duration == 1 ? '' : 's' %> |
<% end -%>

## Actions

<% if @representation_session.activity_log_entries.empty? -%>
No actions were performed during this session.
<% else -%>
<%= raw @representation_session.representative_user.display_name %> performed the following actions on behalf of <%= raw @representation_session.superagent.name %>:

| # | Time | Action | Item | Studio |
|---|------|--------|------|--------|
<% @representation_session.activity_log_entries.each.with_index do |activity_item, i| -%>
| <%= i + 1 %> | <%= activity_item[:happened_at].to_time.in_time_zone(@current_superagent.timezone.name).strftime('%H:%M %Z') %> | <%= activity_item[:verb_phrase] %> | [<%= activity_item[:main_resource].class.to_s %> `<%= activity_item[:main_resource].truncated_id %>`](<%= activity_item[:main_resource].path %>) | <% if activity_item[:superagent] %>[<%= raw activity_item[:superagent].name %>](<%= activity_item[:superagent].path %>)<% end %> |
<% end -%>
<% end -%>

<%= render 'shared/backlinks_section', resource: @representation_session %>
<%= render 'shared/comments_section', resource: @representation_session %>

## [Actions](<%= @representation_session.path %>/actions)

* [`add_comment(text)`](<%= @representation_session.path %>/actions/add_comment) - Add a comment to this session
