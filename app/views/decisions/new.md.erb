# New Decision

Decisions require the following:

* A question that the decision will answer.
* An optional description of the decision (markdown).
* A deadline when the decision will be finalized and closed.
* Optionally, `proposer_eligibility` and `voter_eligibility` — user sets naming who may add options and who may vote. Omit either to leave it open to the whole collective.

Options can be added to the list after the decision is created.

<% if @current_collective.id == @current_tenant.main_collective_id %>
**Visibility:** This decision will be publicly visible.
<% elsif @current_collective.private_workspace? %>
**Visibility:** Only you can access this decision.
<% else %>
**Visibility:** Only members of this collective can access this decision.
<% end %>

