# Decision Settings

Current settings for decision **<%= @decision.question %>**:

| Setting | Value |
|---------|-------|
| Question | <%= @decision.question %> |
| Description | <%= @decision.description || "(none)" %> |
| Options Open | <%= @decision.options_open? ? "Yes" : "No" %> |
| Deadline | <%= @decision.deadline %> |

## [Actions](<%= @decision.path %>/settings/actions)

* [`update_decision_settings(question, description, options_open, deadline)`](<%= @decision.path %>/settings/actions/update_decision_settings) - Update decision settings.
<% if @is_pinned %>* [`unpin_decision()`](<%= @decision.path %>/settings/actions/unpin_decision) - Unpin this decision from the studio homepage.
<% else %>* [`pin_decision()`](<%= @decision.path %>/settings/actions/pin_decision) - Pin this decision to the studio homepage.
<% end %><% if @current_tenant.allow_file_uploads? && @current_studio.allow_file_uploads? %>* [`add_attachment(file)`](<%= @decision.path %>/settings/actions/add_attachment) - Add a file attachment to this decision
<% end %>
