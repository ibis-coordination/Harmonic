# Data Imports

[Back to Subdomain Admin](/subdomain-admin)

All imports for `<%= @current_tenant.subdomain %>.<%= ENV['HOSTNAME'] %>` (<%= @imports.count %> total)

<% if @imports.any? -%>
| Created | Status | Imported By | Collective | |
|---------|--------|-------------|------------|---|
<% @imports.each do |import| -%>
| <%= import.created_at.strftime("%Y-%m-%d %H:%M") %> | <%= import.status %> | <%= import.user.name %> | <%= import.collective&.name || "—" %> | [View](/subdomain-admin/imports/<%= import.id %>) |
<% end -%>
<% else -%>
No imports yet.
<% end %>
